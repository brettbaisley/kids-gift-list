import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className="site-header">
            <h1>Gifts</h1>
            <button>Sign In</button>
        </header>
    )
}

export default Header